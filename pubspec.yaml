name: pmos_enclaire
description: "PMOS ENclaire mobile application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  fl_chart: 1.2.0
  flutter:
    sdk: flutter
  table_calendar: 3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/pomi_logo.png
