# Core

Shared Flutter infrastructure belongs here, including:

- app routing and navigation
- themes and design tokens
- networking and API errors
- configuration and environment handling
- reusable utilities that are not tied to one feature

Feature-specific code must stay under `lib/features/`.
